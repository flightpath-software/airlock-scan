Add the M5 evaluation harness: a labeled corpus under `corpus/` (clean / trigger-word-heavy clean / adversarial / harness-targeted) and `cscan-helper eval`, which runs the Tier-2 reviewer over the corpus and reports **detection rate**, the headline **canary false-positive rate** on clean/trigger files, and **harness attribution accuracy** (Markdown or `--json`). The backend is pluggable: a real model, an offline `--fake` baseline, or an offline `--heuristic` backend that fires on any tool-name mention to illustrate the over-defense failure mode.
