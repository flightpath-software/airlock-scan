Add the project plan and deferred-backlog docs for the two-tier, injection-resistant repo/skill vetting pipeline (authoritative deterministic gate + Dual-LLM quarantine + canary tripwires with harness fingerprinting), reconciled against the early SPEC: cloud-default-but-pluggable LLM backend, file-primary persistence under ~/cscan with a rebuildable SQLite index, and YARA captured as future work.
