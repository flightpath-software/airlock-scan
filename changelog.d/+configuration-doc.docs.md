Add `docs/configuration.md` — a full configuration reference: the four config sources and their precedence, how the API key works (the config holds the env-var *name*, never the secret), the `[tool.airlock]` / `~/airlock/config.toml` / `AIRLOCK_*` surfaces, and copy-paste recipes for switching the LLM provider (OpenAI / Groq / xAI / local) and running offline. Linked from the README.
